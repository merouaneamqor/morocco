{
  "name": "mhka",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Read-only integrity toolkit for the Moroccan History from the Archives knowledge base. Audits; never writes.",
  "bin": { "mhka": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "mhka": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run build && npm test"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^3.2.6"
  }
}

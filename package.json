{
  "name": "moroccan-history-from-the-archives",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "A source-critical historical knowledge base. Public research website built from French, Spanish, Moroccan, British, American, German and Ottoman archival sources.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/normalize.mjs && node scripts/extract-claims.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node scripts/sync.mjs",
    "normalize": "node scripts/normalize.mjs",
    "check": "node scripts/integrity.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "verify": "npm run build && node scripts/integrity.mjs && npm test"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.0",
    "astro": "^5.13.0",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@notionhq/client": "^2.2.15"
  }
}
